<h1>Search Results</h1>

<% if @users.any? %>
  <ul>
    <% @users.each do |user| %>
      <li>
        <%= link_to user.username, profile_path(user.profile) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No users found.</p>
<% end %>