<h1> Hello to the Social Media! <h1>

<body>
    <% if user_signed_in? %>
        <div> Welcome <%= current_user.username %>! </div>
        <%= button_to "Sign out", destroy_user_session_path, method: :delete %> 
    <% else %>
        <div> Care to <%= button_to "Sign in", new_user_session_path %>? </div>
    <% end %>
</body>